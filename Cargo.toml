[package]
name = "neurotic"
version = "0.1.0"
edition = "2021"

[package.metadata.napi]
name = "neurotic"

[lib]
crate-type = ["cdylib"]
path = "native/lib.rs"

[dependencies]
napi = { version = "2", features = ["napi4"] }
napi-derive = "2"

[build-dependencies]
napi-build = "2"
